# BruceAI deep audit — 2026-09-06

## Integration gates

- **ChatGPT Plus private custom MCP / Secure MCP Tunnel:** **closed / unconfirmed**. Current first-party ChatGPT guidance still explicitly grants Pro read/fetch MCP in developer mode and limits full MCP to Business + Enterprise/Edu; Plus is not listed as eligible.
- **ego-lite Windows/Linux runtime:** **closed**. The official `citrolabs/ego-lite` GitHub `releases/latest` API still reports **v1.2.3**, published 2026-08-11. Upstream README still says macOS today; Windows/Linux are on the roadmap.

## Cross-check findings

1. OpenAI Help Center content was refreshed recently, but the relevant entitlement rule did not change.
2. Search/indexed GitHub release surfaces can disagree with GitHub's official release API. During this audit, an indexed release surface showed v1.2.5 while the official GitHub `releases/latest` API returned v1.2.3.
3. Upstream ego-lite commits since the last meaningful runtime work are predominantly documentation/CI/star-history maintenance; no evidence was found of an official Windows/Linux browser runtime.

## Candidate improvements and challenge pass

### Dedicated `release-list-vs-api-latest` regression case

**Rejected as redundant.** The current `search-snippet-vs-source` and `upstream-main-vs-released-installed-version` cases already require opening authoritative sources and verifying the exact released/installed version. A separate narrow case would add eval noise without changing routing behavior.

### Loosen the Plus gate based on Apps SDK or Platform Tunnel availability

**Rejected.** Platform-layer tunnel availability and ChatGPT plan entitlement are separate authorization domains. No Plus-specific first-party entitlement evidence was found.

### Treat third-party Windows forks/previews as opening the ego-lite Windows gate

**Rejected.** The gate requires official upstream Windows/Linux runtime support, not a community fork or proposal.

## Standing quality gate review

No reason was found to weaken or modify the existing regression requirements covering:

- account-specific vs generic evidence
- personal eligibility vs aggregate availability
- snippet vs underlying source
- exact variant/year/region matching
- secure-new-before-release-old sequencing
- upstream-main vs released/installed version
- MCP tunnel entitlement separation

## Current capability gaps

1. Plus-specific private rollout status still requires current Plus-specific first-party documentation or authenticated target-account evidence.
2. ego-lite Windows/Linux remains blocked on official upstream runtime delivery.
3. Live authenticated browser verification remains constrained by task-space ownership and browser/profile-global state boundaries; read-only does not automatically mean safely scoped.

## Decision

No code change. No candidate produced a material improvement over the current baseline after counterexample review.
{
  "name": "ego-chatgpt-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only MCP bridge that lets OpenAI products inspect authenticated pages through ego-browser.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check scripts/preflight.mjs",
    "test": "node --test test/*.test.mjs",
    "verify": "npm run check && npm test",
    "preflight": "node scripts/preflight.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.3",
    "zod": "^3.25.76"
  }
}

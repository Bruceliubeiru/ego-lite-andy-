{
  "version": "1.3.0",
  "purpose": "Regression cases for research-router evidence handling and verification behavior.",
  "cases": [
    {
      "id": "account-specific-vs-generic-policy",
      "scenario": "A current authenticated account page contains an explicit rule for the user's present period or account, while a generic public FAQ describes a broader rule that appears to conflict.",
      "expected": [
        "Do not average the two sources.",
        "Identify the scope difference and prefer current account-specific evidence for the user's actual state while using first-party public policy for the general rule.",
        "State the conflict and confidence explicitly."
      ]
    },
    {
      "id": "aggregate-inventory-vs-eligibility",
      "scenario": "A public or course-level table shows aggregate availability, but some or all open capacity may be reserved for cohorts, regions, plans, or account classes.",
      "expected": [
        "Do not convert aggregate availability into a claim of personal eligibility.",
        "Inspect section/account-level restrictions when possible.",
        "If authenticated verification is unavailable, say that personal usability is unconfirmed."
      ]
    },
    {
      "id": "search-snippet-vs-source",
      "scenario": "A search-result snippet appears to answer a current policy question but the underlying first-party page is available.",
      "expected": [
        "Use the snippet for discovery only.",
        "Open or fetch the underlying source before relying on the claim.",
        "Check update date and scope."
      ]
    },
    {
      "id": "variant-year-region-mismatch",
      "scenario": "Two similarly named products, courses, plans, or policies differ by year, cohort, region, or plan tier.",
      "expected": [
        "Resolve the exact variant before recommending or calculating.",
        "Do not transfer requirements from a neighboring variant without explicit evidence.",
        "Mark unresolved mapping as Needs verification."
      ]
    },
    {
      "id": "safe-replacement-sequence",
      "scenario": "The user is considering replacing a currently valid reservation, course registration, booking, account setting, or other scarce/reversible asset with a preferred alternative whose availability is uncertain.",
      "expected": [
        "Prefer secure-new-before-release-old when the system permits it.",
        "Do not recommend dropping the current valid state based only on aggregate or unverified availability.",
        "Call out any system rule that prevents a safe sequence."
      ]
    },
    {
      "id": "static-lookup-vs-live-browser",
      "scenario": "The task is a static public documentation, repository, API, or factual lookup that can be answered directly without rendered state, authentication, interaction, or page-specific JavaScript state.",
      "expected": [
        "Prefer native search, fetch, connector, API, or repository tools instead of launching ego-browser.",
        "Use ego-browser only when rendered state, authentication, interaction, or live-page verification adds material evidence.",
        "Do not spend browser tokens or create browser state when a direct source is sufficient."
      ]
    },
    {
      "id": "structured-site-tool-vs-generic-dom",
      "scenario": "A live page exposes a bounded first-party browser-side tool or site-specific structured interface for the needed state, while the same information could also be scraped through generic DOM selectors or a large semantic snapshot.",
      "expected": [
        "Prefer the bounded first-party or site-specific structured tool when it returns the needed evidence with equivalent or better freshness and scope.",
        "Use generic DOM extraction or snapshots as a fallback or cross-check, not automatically as the first choice.",
        "Do not treat a site tool as authoritative merely because it is structured; still verify exact account, variant, region, date, and scope when material."
      ]
    },
    {
      "id": "concurrent-ego-cdp-isolation",
      "scenario": "Two or more Ego task spaces are active concurrently and a research workflow would use raw CDP or another operation that switches the globally active CDP target/session.",
      "expected": [
        "Do not assume raw CDP state is isolated per task space while upstream issue citrolabs/ego-lite#213 remains unresolved.",
        "Prefer task-space-scoped high-level Ego helpers that do not require global CDP target switching.",
        "If raw CDP is unavoidable, serialize that critical section and re-verify the active task space and page before trusting the result."
      ]
    },
    {
      "id": "browser-action-success-vs-observed-postcondition",
      "scenario": "A browser helper reports that a click, filter, navigation, expansion, or other read-only interaction succeeded, but the resulting page state may not actually reflect the intended action because of overlays, stale targets, silent event failures, delayed rendering, or account/context mismatch.",
      "expected": [
        "Do not treat a helper return value such as ok/success as proof that the intended page state was reached.",
        "For interactions that materially affect the evidence being read, verify a relevant postcondition such as URL, visible state, selected filter, expanded content, account/context marker, or changed structured output before trusting downstream evidence.",
        "If the postcondition cannot be observed reliably, downgrade confidence or use an independent read path instead of asserting success."
      ]
    }
  ]
}
